<br>
<% if @post.errors.any? %>
    <div id="error_explanation">
        <h2> Hubo <%= pluralize(@post.errors.count, "error(es)") %> lo cual no perimitió el registro de tu post:</h2>
        <ul>
            <% @post.errors.full_messages.each do |msg| %>
                <li><%= msg %>.</li>
            <% end %>
        </ul>
        <%= link_to("Corregir", "/") %>
    </div>
    <% else %>
        <h1> Registraste tu post con éxito </h1>
        <%= link_to("Revisar listado de Posts", "/") %>
<% end %>